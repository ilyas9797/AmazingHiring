*1 задание:*
+1. логирование переделать на модуль logging
+2. формирование запросов вынести в отдельный метод: не нравятся числовые константы
+3. там где возможно изменить тип с Varchar на Int
+4. сделать возможность указания название файл БД и CSV через аргументы запуска, текущие значения оставить как значения по-умолчанию
+5. строки 55-56: есть ли возможность обойтись без подобного replace? если есть, переделать
+6. строка 58: зачем?
+7. строки 34-35: дублирование кода, что будет, если x.replace(‘“’, ‘“”’) не поможет? исправить

Затраченное время: работал понемного несколько дней, поэтому вышло довольно долго 

1) SQL для создания схемы базы данных - представленно атрибутами '_table_schema' и 'sql_command_create_table' класса 'Table' в модуле самого скрипта 'task1-sqlite.py'

2) Файл с указанием зависимостей - не добавлен, ввиду отсутствия зависимостей
